import { PendingPage } from "@/components/common/pending-page";
export default function BeforeAfterPage() { return <PendingPage title="Before and after" description="Compare site conditions visually once project photo APIs are available." capabilities={["Before and after pairing", "Comparison slider", "Project and site context"]} />; }
