import { PendingPage } from "@/components/common/pending-page";
export default function SitesPage() { return <PendingPage title="Sites" description="Organize work locations, supervisors, addresses, coordinates, and site status." capabilities={["Site details and supervisor", "Address and coordinates", "Map link preparation"]} />; }
