import { PendingPage } from "@/components/common/pending-page";
export default function LocationPage() { return <PendingPage title="Project locations" description="Prepare project addresses and coordinates for field teams." capabilities={["Address and coordinates", "Open in Google Maps", "Map integration when project location API exists"]} />; }
