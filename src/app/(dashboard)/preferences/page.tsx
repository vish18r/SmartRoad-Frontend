"use client";
import { useEffect, useState } from "react";
import { useTheme } from "@/components/ui/theme-provider";
export default function PreferencesPage() { const { theme, setTheme } = useTheme(); const [dateFormat, setDateFormat] = useState("DD/MM/YYYY"); const [currency, setCurrency] = useState("INR"); const [pageSize, setPageSize] = useState("25"); useEffect(() => { setDateFormat(localStorage.getItem("smartroad_date_format") || "DD/MM/YYYY"); setCurrency(localStorage.getItem("smartroad_currency") || "INR"); setPageSize(localStorage.getItem("smartroad_page_size") || "25"); }, []); const save = (key: string, value: string) => { localStorage.setItem(key, value); }; return <div className="max-w-2xl"><p className="text-sm font-medium text-orange-600">Workspace</p><h1 className="mt-1 text-3xl font-bold">Preferences</h1><p className="mt-1 text-sm text-slate-500">These settings are stored locally until preference APIs are available.</p><section className="mt-7 grid gap-4 rounded-xl border border-slate-200 bg-white p-6"><label className="grid gap-1 text-sm font-semibold">Theme<select value={theme} onChange={(event) => setTheme(event.target.value as "light" | "dark" | "system")} className="rounded-lg border border-slate-300 px-3 py-2 font-normal"><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label><label className="grid gap-1 text-sm font-semibold">Date format<select value={dateFormat} onChange={(event) => { setDateFormat(event.target.value); save("smartroad_date_format", event.target.value); }} className="rounded-lg border border-slate-300 px-3 py-2 font-normal"><option>DD/MM/YYYY</option><option>MM/DD/YYYY</option><option>YYYY-MM-DD</option></select></label><label className="grid gap-1 text-sm font-semibold">Currency<select value={currency} onChange={(event) => { setCurrency(event.target.value); save("smartroad_currency", event.target.value); }} className="rounded-lg border border-slate-300 px-3 py-2 font-normal"><option>INR</option><option>USD</option><option>EUR</option></select></label><label className="grid gap-1 text-sm font-semibold">Table page size<select value={pageSize} onChange={(event) => { setPageSize(event.target.value); save("smartroad_page_size", event.target.value); }} className="rounded-lg border border-slate-300 px-3 py-2 font-normal"><option>10</option><option>25</option><option>50</option><option>100</option></select></label></section></div>; }
