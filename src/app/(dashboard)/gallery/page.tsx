import { PendingPage } from "@/components/common/pending-page";
export default function GalleryPage() { return <PendingPage title="Photo gallery" description="Capture before-and-after progress for projects and daily work." capabilities={["Multiple image upload", "Full-screen preview", "Before and after comparison"]} />; }
