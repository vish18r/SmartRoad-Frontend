import { PendingPage } from "@/components/common/pending-page";
export default function ConcretePage() { return <PendingPage title="Concrete work" description="Track concrete sections, quantities, materials, labour, machines, supervisors, and completion." capabilities={["Volume and completed quantity", "Material breakdown", "Completion percentage"]} />; }
