import { PendingPage } from "@/components/common/pending-page";
export default function PayrollPage() { return <PendingPage title="Worker payroll" description="Calculate monthly wages, overtime, advances, deductions, paid amounts, and balance." capabilities={["Monthly payroll view", "Net payable calculation", "Payment status"]} />; }
