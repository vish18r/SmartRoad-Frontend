"use client";

import { useState } from "react";

export function LocationCheck() { const [message, setMessage] = useState("Location verification is ready for the site API."); const locate = () => { if (!navigator.geolocation) { setMessage("Location is not supported by this browser."); return; } setMessage("Requesting current location..."); navigator.geolocation.getCurrentPosition((position) => setMessage(`Current coordinates captured: ${position.coords.latitude.toFixed(5)}, ${position.coords.longitude.toFixed(5)}. Distance verification is pending backend site coordinates.`), () => setMessage("Location permission was not granted.")); }; return <section className="max-w-xl rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold text-slate-900">Geofenced attendance</h2><p className="mt-2 text-sm text-slate-500">Capture current location before marking attendance. The backend currently has no geofence endpoint.</p><button type="button" onClick={locate} className="mt-4 rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white">Capture current location</button><p role="status" className="mt-3 text-sm text-slate-600">{message}</p></section>; }
