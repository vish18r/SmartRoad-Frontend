import { PendingPage } from "@/components/common/pending-page";
export default function ExportCenterPage() { return <PendingPage title="Backup and export center" description="Prepare exports for project, financial, worker, material, and document metadata." capabilities={["Export scope selection", "Format and date filters", "Download history"]} />; }
