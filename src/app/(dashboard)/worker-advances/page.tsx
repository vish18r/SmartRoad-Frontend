import { PendingPage } from "@/components/common/pending-page";
export default function WorkerAdvancesPage() { return <PendingPage title="Worker advances" description="Track advances, recovery amounts, remaining balances, and payment status." capabilities={["Worker and date records", "Recovery tracking", "Outstanding advances"]} />; }
