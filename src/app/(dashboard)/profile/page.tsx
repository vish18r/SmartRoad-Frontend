"use client";
import Link from "next/link";
import { useAuth } from "@/hooks/useAuth";
import { Loading } from "@/components/common/states";
export default function ProfilePage() { const { user, loading } = useAuth(); if (loading) return <Loading />; return <div className="max-w-2xl"><p className="text-sm font-medium text-orange-600">Account</p><h1 className="mt-1 text-3xl font-bold">Profile</h1><p className="mt-1 text-sm text-slate-500">Your identity and account details from the backend.</p><section className="mt-7 rounded-xl border border-slate-200 bg-white p-6"><div className="grid gap-5 sm:grid-cols-2"><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Name</dt><dd className="mt-1 font-semibold">{user ? `${user.firstName} ${user.lastName || ""}`.trim() : "Unavailable"}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Email</dt><dd className="mt-1">{user?.email || "Unavailable"}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Phone</dt><dd className="mt-1">{user?.phoneNumber ? `${user.countryCode || ""} ${user.phoneNumber}` : "Unavailable"}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Role</dt><dd className="mt-1">{user?.role || "Unavailable"}</dd></div></div><Link href="/settings/security" className="mt-7 inline-block rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white">Manage security</Link></section></div>; }
