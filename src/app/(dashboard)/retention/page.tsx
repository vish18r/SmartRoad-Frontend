import { PendingPage } from "@/components/common/pending-page";
export default function RetentionPage() { return <PendingPage title="Retention and security deposit" description="Prepare configurable retention, security deposit, deductions, release dates, and balances." capabilities={["Pending retention", "Release tracking", "Financial breakdown"]} />; }
